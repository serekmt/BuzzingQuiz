<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@color/white"
    android:theme="@android:style/Theme.NoTitleBar"
    tools:context="com.quiz.buzzquiz.HomeActivity"
    android:onClick="pageClicked">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/logo"
        android:layout_marginBottom="40dp"
        android:src="@mipmap/logo"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        style="@style/Base.Theme.AppCompat.Light"
        android:maxHeight="@dimen/abc_action_bar_icon_vertical_padding_material"
        android:maxWidth="@dimen/abc_action_bar_icon_vertical_padding_material" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/name"
        android:src="@mipmap/napis1"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/logo"
        android:background="#ffffffff" />

</RelativeLayout>
